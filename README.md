## Quick Start

1. Clone the repository.

2. Change .env-sample filename to .env and set your preferred model (default is phi3 for low-end hardware).

3. Run docker-compose up -d. 

The system will automatically download your chosen model and start the AI engine!